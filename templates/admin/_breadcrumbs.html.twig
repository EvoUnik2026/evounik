{# templates/admin/_breadcrumbs.html.twig #}
<nav class="bg-slate-50 p-2 rounded-md text-xs text-slate-500">
    <a href="{{ path('app_admin_topics') }}" class="hover:text-indigo-600">Topics</a> /
    <span>{{ topic.title }}</span>
</nav>